<ul class="nav navbar-nav navbar-right">
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            
            <%if !@user.avatar.nil? and !@user.avatar.empty? %>
                <%=image_tag('/img/'+@user.avatar+'.jpg',width:20,alt:'profile_picture')%>
            <% else %>
                <span class="glyphicon glyphicon-user"></span>
            <% end %>
            
            
            
            <strong><%=@user.first_name%></strong>
            <span class="glyphicon glyphicon-chevron-down"></span>
        </a>
        <ul class="dropdown-menu">
            <li>
                <div class="navbar-login">
                    <div class="row">
                        <div class="col-lg-4">
                            <p class="text-center">
                                <%if !@user.avatar.nil? and !@user.avatar.empty? %>
                                    <%=image_tag('/img/'+@user.avatar+'.jpg',width:100,alt:'profile_picture')%>
                                <% else %>
                                    <span class="glyphicon glyphicon-user icon-size"></span>
                                <% end %>
                                
                            </p>
                        </div>
                        <div class="col-lg-8">
                            <p class="text-left"><strong><%=@user.first_name+' '+@user.last_name%></strong></p>
                            <%#<p class="text-left small">correoElectronico@email.com</p>%>
                            <p class="text-left">
                                <a href="/profile/<%=@user.id%>" class="btn btn-primary btn-block btn-sm">Update profile</a>
                            </p>
                        </div>
                    </div>
                </div>
            </li>
            <li class="divider"></li>
            <li>
                <div class="navbar-login navbar-login-session">
                    <div class="row">
                        <div class="col-lg-12">
                            <p>
                                <a href="/logout" class="btn btn-danger btn-block">Cerrar Sesion</a>
                            </p>
                        </div>
                    </div>
                </div>
            </li>
        </ul>
    </li>
</ul>